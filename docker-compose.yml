version: '3.7'

services:

  simple-file-server:
    build: .
    image: flaviostutz/simple-file-server
    ports:
      - "4000:4000"
    environment:
      - LOG_LEVEL=debug
      # - WRITE_SHARED_KEY=abc
      # - READ_SHARED_KEY=123

      